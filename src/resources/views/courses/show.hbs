<div class="mt-4 course-detail">
  <div class="row">
    <!-- Main Content -->
    <div class="col-lg-8">
      <div class="mb-4">
        <a href="/" class="btn btn-outline-secondary btn-sm mb-3">&larr; Quay lại trang chủ</a>
        <h1 class="course-title">{{course.name}}</h1>
        <p class="text-muted">{{course.description}}</p>
      </div>

      <div class="video-container mb-4">
        {{#if isEnrolled}}
        <iframe width="100%" height="450" src="https://www.youtube.com/embed/{{course.videoID}}"
          title="YouTube video player" frameborder="0"
          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
          allowfullscreen>
        </iframe>
        {{else}}
        <div class="text-center py-5 bg-light" style="border: 1px dashed #ccc;">
          <h4 class="text-muted">Bạn cần đăng ký để xem nội dung này</h4>
          <p class="mb-0">Vui lòng đăng ký khóa học để bắt đầu học.</p>
        </div>
        {{/if}}
      </div>

      <div class="course-content">
        <h3>Bạn sẽ học được gì?</h3>
        <ul class="list-check">
          <li>Hiểu rõ kiến thức nền tảng trong {{course.name}}.</li>
          <li>Xây dựng được các ứng dụng thực tế sau khóa học.</li>
          <li>Làm chủ các công cụ và kỹ thuật mới nhất.</li>
          <li>Cơ hội việc làm rộng mở với mức lương hấp dẫn.</li>
        </ul>
      </div>

      <div class="mt-5">
        <h3>Nội dung khóa học</h3>
        <p class="text-muted fst-italic">Danh sách bài học đang được cập nhật...</p>
      </div>
    </div>

    <!-- Sidebar -->
    <div class="col-lg-4">
      <div class="card shadow-sm sticky-top" style="top: 20px; z-index: 1;">
        <img src="{{course.image}}" class="card-img-top" alt="{{course.name}}"
          style="object-fit: cover; height: 200px;">
        <div class="card-body">
          <h5 class="text-center text-primary mb-3">
            {{#if course.price}}
            {{course.price}} VNĐ
            {{else}}
            Miễn phí
            {{/if}}
          </h5>

          {{#if isEnrolled}}
          <button class="btn btn-success btn-block btn-lg mb-3" disabled>ĐÃ ĐĂNG KÝ</button>
          <p class="text-center text-success"><small>Bạn đã sở hữu khóa học này</small></p>
          {{else}}
          <form action="/courses/{{course._id}}/enroll" method="POST">
            <button type="submit" class="btn btn-primary btn-block btn-lg mb-3">
              {{#if course.price}}
              MUA KHÓA HỌC
              {{else}}
              ĐĂNG KÝ HỌC
              {{/if}}
            </button>
          </form>
          {{/if}}

          <ul class="list-group list-group-flush">
            <li class="list-group-item d-flex justify-content-between align-items-center">
              <span class="text-muted"><i class="oi oi-bar-chart mr-2"></i>Trình độ</span>
              <strong>{{course.level}}</strong>
            </li>
            <li class="list-group-item d-flex justify-content-between align-items-center">
              <span class="text-muted"><i class="oi oi-clock mr-2"></i>Thời lượng</span>
              <strong>03 giờ 12 phút</strong>
            </li>
            <li class="list-group-item d-flex justify-content-between align-items-center">
              <span class="text-muted"><i class="oi oi-book mr-2"></i>Tổng số bài học</span>
              <strong>12 bài</strong>
            </li>
            <li class="list-group-item d-flex justify-content-between align-items-center">
              <span class="text-muted"><i class="oi oi-infinity mr-2"></i>Truy cập</span>
              <strong>Trọn đời</strong>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>